# mysql
spring:
  datasource:
    #MySQL配置
    driverClassName:  com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/librarysystem
    username: root
    password: 123456
  # 自动更新
  jpa:
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:Mapper/*.xml
  type-aliases-package: com.example.demo.Entity
server:
  port: 8090

